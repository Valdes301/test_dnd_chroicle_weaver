datasource db {
  provider = "sqlite"
  url      = "file:/app/data/dev.db"
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-arm64-openssl-3.0.x"]
}

// ... i tuoi modelli (Campaign, Session, ecc.) rimangono invariati
